// Flip to false to make content links navigate in the same tab instead.
// Applies to WikiLink/Ref (in-article [[wikilink]] renders), graph nodes,
// and search results — not SiteHeader's primary nav, which stays same-tab.
export const OPEN_CONTENT_LINKS_IN_NEW_TAB = true;
